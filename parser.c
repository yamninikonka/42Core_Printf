

void validate_format(const char *format)
{
    if (format == NULL)
    {
        fprintf(stderr, "Error: Format string is NULL\n");
        return;
    }
    if (strlen(format) == 0)
    {
        
    }
}